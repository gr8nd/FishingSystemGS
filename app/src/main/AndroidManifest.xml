<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission-sdk-23 android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission-sdk-23 android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FishingSystemGS"
        tools:targetApi="31">
        <activity
            android:name=".activities.LauncherActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.AdminActivity"
            android:exported="false"
            android:label="@string/admin_panel" />
        <activity
            android:name=".activities.ViewTSGActivity"
            android:exported="false"
            android:label="@string/view_tsg" />
        <activity
            android:name=".activities.ViewKVSActivity"
            android:exported="false"
            android:label="@string/view_ksv" />
        <activity
            android:name=".activities.ViewTSActivity"
            android:exported="false"
            android:label="@string/view_tss" />
        <activity
            android:name=".activities.ViewPGRActivity"
            android:exported="false"
            android:label="@string/view_pgr" />
        <activity
            android:name=".activities.MainActivity"/>

    </application>

</manifest>